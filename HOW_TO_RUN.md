# 🚀 كيف تشغّل SmartInstall - دليل بسيط جداً

---

## ⚠️ **هام جداً: موقع الملفات**

الملفات موجودة في المسار التالي على جهازك:

```
📁 Downloads (أو المكان الذي حمّلت فيه الملفات)
   └── 📁 SmartInstall
       ├── 🚀 RUN.bat          ← هذا الملف للتشغيل السريع
       ├── 🐍 main.py          ← هذا الملف الأساسي
       ├── 📄 requirements.txt ← قائمة المكتبات
       └── ...
```

---

## 📥 **الخطوة 1: تحميل الملفات**

أنت الآن تتحدث معي في واجهة Claude، والملفات موجودة في النظام ولكن **يجب تحميلها على جهازك أولاً**.

### **كيف تحمّل المشروع كاملاً:**

1. **اضغط هنا لتحميل مجلد SmartInstall كاملاً:**
   - ابحث عن زر "Download" أو "تحميل" في واجهة Claude
   - أو استخدم الروابط التي سأوفرها لك

2. **حمّل المجلد وفك الضغط:**
   - سيتم تحميل ملف مضغوط (ZIP)
   - افتحه وفك الضغط
   - ستجد مجلد اسمه `SmartInstall`

---

## 🖥️ **الخطوة 2: التشغيل (بعد التحميل)**

### **الطريقة الأسهل - Windows:**

```
1. افتح مجلد SmartInstall على جهازك
2. ابحث عن ملف اسمه: RUN.bat
3. انقر عليه نقراً مزدوجاً (Double Click)
4. انتظر... سيفتح التطبيق! ✅
```

### **بديل PowerShell**

يمكنك تشغيل السكريبت الخاص بـ PowerShell بدلاً من `RUN.bat`:

```powershell
.\run.ps1
```

كما يوجد سكريبت لنظام Linux/macOS:

```bash
./run.sh
```

ملاحظة: تم تفعيل ميزة "Raptor mini (Preview)" لجميع العملاء في `config.py` تحت `FeatureFlags.RAPTOR_MINI_ENABLED = True`.

### **الطريقة البديلة - Command Prompt:**

```bash
# 1. افتح Command Prompt (اضغط Win + R ثم اكتب cmd)

# 2. انتقل لمجلد SmartInstall
cd C:\Users\YourName\Downloads\SmartInstall

# 3. ثبّت المكتبات (أول مرة فقط)
pip install -r requirements.txt

# 4. شغّل التطبيق
python main.py
```

---

## ❌ **المشكلة الشائعة: "لا يوجد ملف"**

### **السبب:**
أنت لم تحمّل الملفات على جهازك بعد!

### **الحل:**

#### **أنا الآن سأوفر لك الملفات بطريقتين:**

---

## 📦 **طريقة التحميل:**

نظراً لأنك في واجهة Claude Chat، الملفات موجودة في النظام السحابي.

### **خياراتك:**

#### **الخيار 1: نسخ الكود يدوياً (للملفات الصغيرة)**

سأعطيك كل ملف على حدة وتنسخه يدوياً:

1. أنشئ مجلد على جهازك اسمه `SmartInstall`
2. داخله، أنشئ الملفات التالية:

**ملف: main.py**
```python
# سأعطيك الكود كاملاً...
```

#### **الخيار 2: تحميل من GitHub (الأسهل)**

إذا كانت الملفات على GitHub، حمّلها مباشرة.

#### **الخيار 3: استخدام رابط التحميل**

إذا كان متاحاً في واجهة Claude.

---

## 🎯 **الحل الفوري:**

دعني أنشئ لك ملف تشغيل مبسط جداً يمكنك نسخه:

### **أنشئ ملف جديد اسمه: `quick_start.py`**

```python
import os
import sys

print("=" * 60)
print("  SmartInstall - Quick Start")
print("=" * 60)
print()

# التحقق من Python
print("✓ Python version:", sys.version)
print()

# التحقق من المكتبات
required = ['PyQt5', 'psycopg2', 'bcrypt']
missing = []

for lib in required:
    try:
        __import__(lib)
        print(f"✓ {lib} installed")
    except ImportError:
        print(f"✗ {lib} NOT installed")
        missing.append(lib)

print()

if missing:
    print("⚠️  Installing missing libraries...")
    os.system(f"pip install {' '.join(missing)}")

print()
print("▶️  Starting SmartInstall...")
print()

# تشغيل التطبيق
try:
    import main
except Exception as e:
    print(f"❌ Error: {e}")
    print()
    print("Please check if main.py exists in the same folder.")
```

---

## 📝 **ملخص المشكلة والحل:**

### **المشكلة:**
```
أنت الآن في واجهة Claude Chat
الملفات موجودة في نظام Claude
لكنها ليست على جهازك بعد ❌
```

### **الحل:**
```
1. يجب تحميل الملفات على جهازك أولاً
2. بعد التحميل، شغّل RUN.bat
3. أو استخدم: python main.py
```

---

## 🆘 **هل تريد المساعدة؟**

### **أخبرني:**

1. **هل حمّلت المشروع على جهازك؟**
   - نعم → أين حفظته؟
   - لا → سأساعدك في التحميل

2. **ما نظام التشغيل؟**
   - Windows
   - Mac
   - Linux

3. **هل Python مثبت؟**
   - نعم → اكتب في CMD: `python --version`
   - لا → سأعطيك رابط التحميل

---

## 💡 **الخطوة التالية:**

**أخبرني بالضبط أين أنت في العملية:**

```
[ ] لم أحمّل المشروع بعد
[ ] حمّلت المشروع لكن لا أعرف كيف أفتحه
[ ] فتحت المجلد لكن لا أجد RUN.bat
[ ] وجدت RUN.bat لكن لا يعمل
[ ] مشكلة أخرى (اشرحها)
```

**سأساعدك حسب موقعك! 🚀**
