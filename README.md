# 🎉 SmartInstall - نظام إدارة المبيعات بالتقسيط الاحترافي

## 📋 نظرة عامة

**SmartInstall** هو نظام Point-of-Sale (POS) احترافي متكامل لإدارة مبيعات الأجهزة الكهربائية بالتقسيط للشركات التي تبيع بالتجزئة.

### **🏆 المميزات الاحترافية:**

✅ **Offline-First Architecture**
- يعمل بدون إنترنت بالكامل
- قاعدة بيانات محلية (PostgreSQL/SQLite)
- مزامنة تلقائية عند توفر الإنترنت

✅ **Multi-Branch Support**
- دعم فروع متعددة
- تقارير موحدة للإدارة
- مزامنة بين الفروع

✅ **Remote Management**
- المدير يمكنه الدخول من أي مكان
- Dashboard في الوقت الفعلي
- إشعارات فورية

✅ **Retail Best Practices**
- معايير صناعة التجزئة
- POS System محترف
- Inventory Management متقدم

---

## ✅ ما تم إنجازه (المرحلة 1: البنية التحتية)

### **الملفات المُنشأة:**

```
SmartInstall/
├── .env.example                    ✅ مثال المتغيرات البيئية
├── requirements.txt                ✅ المكتبات المطلوبة
├── config.py                       ✅ الإعدادات المركزية
├── database/
│   ├── __init__.py                 ✅
│   ├── schema.sql                  ✅ بنية قاعدة البيانات الكاملة (8 جداول)
│   └── db_manager.py               ✅ مدير الاتصال بقاعدة البيانات
└── README.md                       ✅ هذا الملف
```

### **ما تم تنفيذه:**

✅ **قاعدة البيانات الكاملة (8 جداول):**
- Users (المستخدمون)
- Customers (العملاء)
- Categories (الفئات)
- Inventory (المخزون)
- Installments (العقود)
- Payments (الدفعات)
- AuditLog (سجل التدقيق)
- SystemSettings (الإعدادات)

✅ **Views جاهزة:**
- OverduePayments (الأقساط المتأخرة)
- ContractsSummary (ملخص العقود)
- InventoryStatus (حالة المخزون)
- DailySalesStats (إحصائيات يومية)

✅ **DatabaseManager كامل:**
- Connection Pool
- Context Manager
- معالجة الأخطاء
- اختبار الاتصال

✅ **نظام إعدادات شامل:**
- AppConfig
- DatabaseConfig
- SecurityConfig
- PermissionsConfig (RBAC كامل)
- UIConfig
- وأكثر...

---

## 🚀 الخطوات التالية (لإكمال التطبيق)

### **المرحلة 2: Models (نماذج البيانات)**

يجب إنشاء:
```
models/
├── __init__.py
├── user.py                    # نموذج المستخدم
├── customer.py                # نموذج العميل
├── inventory.py               # نموذج الجهاز
├── installment.py             # نموذج العقد
└── payment.py                 # نموذج الدفعة
```

### **المرحلة 3: Controllers (المنطق البرمجي)**

يجب إنشاء:
```
controllers/
├── __init__.py
├── auth_controller.py         # تسجيل الدخول
├── customer_controller.py     # إدارة العملاء
├── inventory_controller.py    # إدارة المخزون
├── installment_controller.py  # إدارة العقود
├── payment_controller.py      # إدارة الدفعات
├── report_controller.py       # التقارير
└── auto_debit_controller.py   # السحب التلقائي
```

### **المرحلة 4: Utils (الوظائف المساعدة)**

يجب إنشاء:
```
utils/
├── __init__.py
├── validators.py              # التحقق من البيانات
├── encryption.py              # التشفير
├── error_handler.py           # معالجة الأخطاء
├── logger.py                  # Logging
├── notifications.py           # الإشعارات
├── backup_manager.py          # النسخ الاحتياطي
└── exporters/
    ├── __init__.py
    ├── pdf_exporter.py
    ├── excel_exporter.py
    └── word_exporter.py
```

### **المرحلة 5: Views (الواجهة - PyQt5)**

يجب إنشاء:
```
views/
├── __init__.py
├── styles.qss                 # تنسيق الواجهة
├── dialogs/
│   ├── login_dialog.py
│   ├── customer_dialog.py
│   └── payment_dialog.py
├── windows/
│   ├── main_window.py
│   ├── dashboard_window.py
│   ├── customers_window.py
│   ├── inventory_window.py
│   ├── installments_window.py
│   ├── payments_window.py
│   ├── reports_window.py
│   └── settings_window.py
└── widgets/
    ├── custom_table.py
    └── custom_buttons.py
```

### **المرحلة 6: main.py**

يجب إنشاء:
```python
# main.py - نقطة البداية
```

---

## 📦 التثبيت والإعداد

### **1. متطلبات النظام:**
- Windows 10/11
- Python 3.8+
- PostgreSQL 12+

### **2. تثبيت Python:**
```bash
# تحقق من إصدار Python
python --version

# يجب أن يكون 3.8 أو أعلى
```

### **3. تثبيت PostgreSQL:**
- حمّل من: https://www.postgresql.org/download/
- أو استخدم خدمة سحابية (ElephantSQL, Supabase, إلخ)

### **4. إعداد المشروع:**

```bash
# 1. انتقل لمجلد المشروع
cd SmartInstall

# 2. أنشئ بيئة افتراضية (موصى به)
python -m venv venv

# 3. فعّل البيئة الافتراضية
# Windows:
venv\Scripts\activate

# Linux/Mac:
source venv/bin/activate

# 4. ثبّت المكتبات
pip install -r requirements.txt --break-system-packages
```

### **تشغيل التطبيق (Run)**

بعد التثبيت يمكنك تشغيل التطبيق بإحدى الطرق التالية:

- عبر ملف الدُفعة على Windows:

```bat
RUN.bat
```

- عبر PowerShell (مضاف):

```powershell
.\run.ps1
```

- عبر Unix-like systems:

```bash
./run.sh
```

ملاحظة: تم تفعيل ميزة "Raptor mini (Preview)" لجميع العملاء عبر `config.py` في `FeatureFlags.RAPTOR_MINI_ENABLED = True`.

### **5. إعداد قاعدة البيانات:**

```bash
# 1. انسخ ملف .env
cp .env.example .env

# 2. عدّل .env بمعلومات قاعدة بياناتك
# استخدم محرر نصوص

# 3. أنشئ قاعدة البيانات
psql -U postgres
CREATE DATABASE smartinstall_db;
\q

# 4. نفّذ schema.sql
psql -U postgres -d smartinstall_db -f database/schema.sql
```

### **6. اختبار الاتصال:**

```bash
# اختبر db_manager.py
python database/db_manager.py

# يجب أن ترى:
# ✅ Database initialized successfully
```

---

## 🔑 تو

ليد مفاتيح التشفير

```python
# افتح Python وشغّل:
from cryptography.fernet import Fernet
import secrets

# مفتاح Fernet
print("ENCRYPTION_KEY=" + Fernet.generate_key().decode())

# مفتاح التطبيق
print("APP_SECRET_KEY=" + secrets.token_urlsafe(32))

# انسخ النتائج إلى ملف .env
```

---

## 📚 المراجع الإضافية

### **البرومبت الكامل:**
راجع الملفات التالية من المجلد الرئيسي:
- `FINAL_PROMPT_READY_TO_USE.md` - البرومبت المختصر
- `SmartInstall_Enhanced_Prompt.md` - البرومبت المفصل الكامل

### **استراتيجية التطوير للموبايل:**
- `Mobile_Migration_Strategy.md` - للمستقبل

---

## ⚠️ ملاحظات مهمة

1. **لا ترفع ملف .env إلى Git!**
   ```bash
   echo ".env" >> .gitignore
   ```

2. **غيّر كلمات المرور الافتراضية**

3. **استخدم SSL للاتصالات السحابية**
   ```
   DB_SSL_MODE=require
   ```

4. **احفظ نسخ احتياطية دورية**

---

## 🎯 الحالة الحالية

| المرحلة | الحالة | النسبة |
|:---|:---:|:---:|
| البنية التحتية | ✅ مكتمل | 100% |
| Models | ⏳ قيد الإنشاء | 0% |
| Controllers | ⏳ قيد الإنشاء | 0% |
| Utils | ⏳ قيد الإنشاء | 0% |
| Views | ⏳ قيد الإنشاء | 0% |
| main.py | ⏳ قيد الإنشاء | 0% |
| **الإجمالي** | 🚧 **قيد البناء** | **~15%** |

---

## 💡 كيفية المتابعة

### **الخيار 1: إكمال يدوياً**
استخدم البرومبت الكامل كمرجع وابنِ الملفات المتبقية يدوياً.

### **الخيار 2: استخدام Claude** (موصى به)
1. افتح محادثة جديدة مع Claude
2. أرفق ملف `SmartInstall_Enhanced_Prompt.md`
3. اكتب: "أكمل بناء المشروع من المرحلة 2 (Models)"

### **الخيار 3: استخدام البرومبت المختصر**
استخدم `FINAL_PROMPT_READY_TO_USE.md` للبناء السريع.

---

## 📞 الدعم

للمزيد من المعلومات، راجع:
- `INDEX.md` - فهرس جميع الملفات
- `HOW_TO_USE.md` - دليل الاستخدام الكامل
- `Project_Summary.md` - ملخص المشروع

---

## 🎉 مبروك!

لقد أكملت **المرحلة الأولى** بنجاح!
البنية التحتية جاهزة ومكتملة 100%.

**الخطوة التالية:** بناء Models ثم Controllers ثم Views.

**حظاً موفقاً! 🚀**
