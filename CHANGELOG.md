# Changelog

All notable changes to this project are documented in this file.

## [Unreleased]
- Enable `Raptor mini (Preview)` feature flag for all clients (`config.py`).
- Add platform run scripts: `run.ps1` (PowerShell), `run.sh` (Unix-like).
- Update documentation: `README.md`, `HOW_TO_RUN.md` with run instructions.
- Minor updates to `RUN.bat` content re-affirmed.

---

## [1.0.0] - Initial baseline
- Project baseline and scaffolding.
